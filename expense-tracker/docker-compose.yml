version: '3.8'

services:
  expense-tracker:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: expense-tracker
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
